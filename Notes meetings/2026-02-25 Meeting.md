Make a list of methods and how easy it is to combine them etc.

Subset of datasets used. 
Make note of size of index.
measure data movement (is this possible in faiss? I guess to check cache utilization)

add nlist (number of cluster from k-means) (i guess to dataset, since its per dataset)