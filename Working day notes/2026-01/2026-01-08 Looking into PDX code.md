Today we'll look into the PDX library. In the end we should decide if we want to write the missing part for the AVX2 implementation ourselves or if we have access to a system with AVX512 support. The latter would be preferred. Less work for us and also a system that very likely has more resources than my own. We would also be able to work from any machine, since we'd have to SSH into the system we use for the benchmarks anyway. (Or at least I hope so xd.) In the end we should write an E-Mail if we decide to ask for a system of theirs.

Ugh I give up for now. I tried to make it run via the Intel SDE (Software Development Emulator), which let's me run code with machine code not natively running on my machine, but I can't get the compilation of the PDX project properly installed. It complains about an import, which in theory should exists in the virtual environment. I also saw that the PDX project requires numpy to be 2.2.2, wehere as FAISS want 1.26.4 I think. F.