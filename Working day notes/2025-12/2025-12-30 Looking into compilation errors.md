Looking at the compilation errors of the pdx library. It seems like it works for avx512 only right now. Or at least it compiles without errors only for an avx512 architecture. It successfully compiles choosing the cascadelake arch, but this obviously won't run on my device. Now I'll look into the avx2 error, maybe there's an easy solution to make it work. The example can't be run right now, since conda and pip have different versions of numpy. There's a runtime error. It complains about the version mismatch. I have to install both libraries with all their dependencies inside the same package manager.