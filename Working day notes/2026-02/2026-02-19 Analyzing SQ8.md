Learned stuff about SQ8, why it works so slow etc. Interesting to see that it's only for memory compression and actually decompresses values before doing distance calculation. ==> massive overhead during search as seen by benchmark. It's been running for 48+h and only ~80% is done.

