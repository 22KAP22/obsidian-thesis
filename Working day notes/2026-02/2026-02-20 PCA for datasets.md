Wrote a small python script computing the PCA transformation matrices, mean of dataset, etc. (all that is needed for PCA). Need to run it after the benchmark.... Thought I might do it on my machine, but remembered I don't have the datasets on it... So I guess I'll only debug it maybe. Will write another script that creates the transformed dataset and then creates indexes with reduced dimensions using PCA. I'm not sure how many of them it makes sense to use, but will experiment with different values.