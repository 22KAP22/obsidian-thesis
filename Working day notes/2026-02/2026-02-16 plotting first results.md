We have some first benchmarking results. Yay!

We'll be using matplotlib to plot them with a jupyter notebook file or whatever they're called to make it somewhat interactive.

I noticed that epsilon0 for the adsampling method is set to 1.5 instead of the recommended 2.1 by the authors of the adsampling paper. Not sure why that is. They even have the 2.1 commented out there.